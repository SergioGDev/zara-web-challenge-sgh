// Props for the component
export type {{ pascalCase name }}Props = {
    // Put here the {{ pascalCase name }}Props. For example:
    exampleProp: string;
};

// Props for the hook
export type Use{{ pascalCase name }}Prop = {
    // Put here the Use{{ pascalCase name }}Prop. For example:
    exampleProp: string;
};
