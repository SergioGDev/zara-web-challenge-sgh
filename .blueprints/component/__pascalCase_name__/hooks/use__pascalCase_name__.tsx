import { Use{{ pascalCase name }}Props } from '../{{ camelCase name }}.types';

export const use{{ pascalCase name }} = ({ exampleProp }: Use{{ pascalCase name }}Props) => {
    // Write here the full functionality of the component (useState, useEffect, etc...)

    return {};
};
