import { {{pascalCase name}}ContextType } from "./{{camelCase name}}Context.types";

export const initial{{pascalCase name}}ContextState: {{pascalCase name}}ContextType = {
    // Add here de initial state of the vars:
    // exampleVarA: false,
    // exampleVarB: '',
};