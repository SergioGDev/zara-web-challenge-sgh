import { UseCardsContainerProps } from '../cardsContainer.types';

export const useCardsContainer = ({ exampleProp }: UseCardsContainerProps) => {
    // Write here the full functionality of the component (useState, useEffect, etc...)

    return {};
};
