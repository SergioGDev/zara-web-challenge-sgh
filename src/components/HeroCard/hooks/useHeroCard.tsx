import { UseHeroCardProps } from '../heroCard.types';

export const useHeroCard = ({ exampleProp }: UseHeroCardProps) => {
    // Write here the full functionality of the component (useState, useEffect, etc...)

    return {};
};
