import { UseFinderInputProps } from '../finderInput.types';

export const useFinderInput = ({ exampleProp }: UseFinderInputProps) => {
    // Write here the full functionality of the component (useState, useEffect, etc...)

    return {};
};
